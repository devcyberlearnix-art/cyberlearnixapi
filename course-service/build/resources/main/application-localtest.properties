# Localtest profile for Course Service
spring.profiles.active=localtest

# Use the same access token secret as the user-service localtest environment
jwt.secret=${JWT_ACCESS_TOKEN_SECRET:localTestSecretKeyThatIsAtLeast32CharactersLong}
jwt.access-token.secret=${JWT_ACCESS_TOKEN_SECRET:localTestSecretKeyThatIsAtLeast32CharactersLong}
jwt.issuer=${JWT_ISSUER:cyberlearnix}
jwt.audience=${JWT_AUDIENCE:cyberlearnix-clients}
